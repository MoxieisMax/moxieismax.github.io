# Affilaite (React port)

Local development:

```bash
cd site-react
npm install
npm run dev
```

Build:

```bash
npm run build
npm run preview
```

Notes:
- This repository contains a Vite + React port of the original Jekyll site.
- Don't commit `node_modules/`; a `.gitignore` is present.
